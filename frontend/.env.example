# API Configuration
# VITE_API_BASE_URL: 빈 값 = 같은 origin (통합 서버). 원격 백엔드 사용 시에만 지정.
VITE_API_BASE_URL=

# 채팅「자유수다」WebSocket — 같은 origin의 /ws/chat 사용 시 빈 값(기본).
# 프론트 전용 호스팅(Netlify)에서 Render 백엔드로 직접 연결할 때만 지정.
# VITE_WS_URL=wss://athletetime-backend.onrender.com/ws/chat
