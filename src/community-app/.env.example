# Copy this file to `.env` or `.env.local` to configure the community app locally
# The backend API endpoint defaults to the production Render deployment.
# Override the value below when running the backend locally (e.g. http://localhost:10000/community).

VITE_COMMUNITY_API_URL=https://athletetime-backend.onrender.com/community
