# Render.com Environment Variables Setup Guide
# ============================================

# Cloudinary Configuration (이미지 CDN)
CLOUDINARY_CLOUD_NAME=dedmfxtpa
CLOUDINARY_API_KEY=374662414448121
CLOUDINARY_API_SECRET=Z7aEbq9Ur538IGfk7q-A8QX72Ac

# JWT Secret (랜덤 생성된 보안 키)
JWT_SECRET=athletetime_jwt_secret_2024_production_$(date +%s)_secure_key

# Server Configuration
PORT=3005
NODE_ENV=production

# bcrypt Configuration
BCRYPT_ROUNDS=10

# Rate Limiting
RATE_LIMIT_POSTS=10
RATE_LIMIT_COMMENTS=30
RATE_LIMIT_VOTES=50

# CORS Configuration
FRONTEND_URL=https://athlete-time.netlify.app
CORS_ORIGIN=https://athlete-time.netlify.app

# PostgreSQL (Render가 자동으로 설정)
# DATABASE_URL=postgresql://... (자동 설정됨)

# ============================================
# 위 환경 변수들을 Render.com에 설정해야 합니다
# ============================================
